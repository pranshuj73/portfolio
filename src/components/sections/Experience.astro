---
import data from "@/data/experience.json";
---
<section>
<h2 class="mb-2">Experience:</h2>
<ul class="flex flex-col gap-4 list-disc list-outside ml-4">
  { data.experience.map((exp) => (
    <li>
      <h3>
        {exp.title} at
        <a class="link" href={exp.company_url} target="_blank" rel="noreferrer noopener">{exp.company}</a>
      </h3>
      <p class="text-sm">{exp.duration} • {exp.location}</p>
    </li>
  )) }
</ul>
</section>